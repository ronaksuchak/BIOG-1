import { WiredNetworkAdapters } from './wired-network-adapters';

describe('WiredNetworkAdapters', () => {
  it('should create an instance', () => {
    expect(new WiredNetworkAdapters()).toBeTruthy();
  });
});
