import { WirelessNetworkAdapters } from './wireless-network-adapters';

describe('WirelessNetworkAdapters', () => {
  it('should create an instance', () => {
    expect(new WirelessNetworkAdapters()).toBeTruthy();
  });
});
