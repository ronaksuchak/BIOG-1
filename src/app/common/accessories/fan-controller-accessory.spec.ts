import { FanControllerAccessory } from './fan-controller-accessory';

describe('FanControllerAccessory', () => {
  it('should create an instance', () => {
    expect(new FanControllerAccessory()).toBeTruthy();
  });
});
