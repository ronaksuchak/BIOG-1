import { ThermalCompoundAccessory } from './thermal-compound-accessory';

describe('ThermalCompoundAccessory', () => {
  it('should create an instance', () => {
    expect(new ThermalCompoundAccessory()).toBeTruthy();
  });
});
