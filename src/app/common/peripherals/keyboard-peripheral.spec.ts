import { KeyboardPeripheral } from './keyboard-peripheral';

describe('KeyboardPeripheral', () => {
  it('should create an instance', () => {
    expect(new KeyboardPeripheral()).toBeTruthy();
  });
});
