import { Product } from "MasterProject/src/app/common/products/product";

}
export  class WebcamPeripheral extends Product{
   id:number;
   resolution:number;
   connection:string;
   WebcamFocusType focusType;.............................enum
   OSType operatingSystem;............................enum
   fovAngle:string;
   
}