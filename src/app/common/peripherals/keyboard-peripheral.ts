import { Product } from "MasterProject/src/app/common/products/product";

export class KeyboardPeripheral extends Product{
    id:number;
     KeyboardStyle style;..................................enum
     switchType:string;
    isTenkeyLess:boolean;
    PeripheralConnectionType connectionType;................enum
     backlit:string;
    
 }
 