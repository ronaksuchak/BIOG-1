import { Product } from "MasterProject/src/app/common/products/product";

 export class MicePeripheral extends Product{
    id:number;
    MiceTrackingMethod trackingMethod;.................enum
    PeripheralConnectionType connectionType;..............enum
   maxDpi:number;
   MiceOrientation handOrientation;......................enum
   
}
